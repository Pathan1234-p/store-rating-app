{
  "name": "store-rating-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm install --prefix backend && npm install --prefix frontend --include=dev && npm run build --prefix frontend",
    "start": "npm run db:init --prefix backend && npm run seed --prefix backend && npm run start --prefix backend"
  },
  "engines": {
    "node": ">=18"
  }
}
